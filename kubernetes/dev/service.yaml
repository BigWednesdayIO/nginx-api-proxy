apiVersion: v1
kind: Service
metadata:
  name: nginx-svc
  labels:
    app: nginx
    env: development
spec:
  ports:
  - port: 443
    protocol: TCP
    name: https
  selector:
    app: nginx
    env: development
  type: LoadBalancer
