machine:
  python:
    version: 2.7.3
  services:
    - docker

checkout:
  post:
    - git submodule update --init
    - echo $(git tag --points-at 07811ef)

dependencies:
  cache_directories:
    - ~/google-cloud-sdk
  override:
    - scripts/ensure_glcoud_installed.sh

test:
  override:
    - echo

deployment:
  tag_test:
    tag: /release-.*/
    commands:
      - echo "Would be deploying from a tag"
  dev:
    branch: master
    commands:
      - scripts/rolling_deploy.sh nginx-api-proxy app=nginx development ${PWD} ${PWD}/kubernetes/rc.json
  prod:
    branch: production
    commands:
      - scripts/rolling_deploy.sh nginx-api-proxy app=nginx production ${PWD} ${PWD}/kubernetes/rc.json
